/* import express from "express";
import {
  getSavedTranslations,
  updateSavedTranslations,
  deleteSavedTranslations,
} from "../controllers/03_userProfileController.js";

const router = express.Router();

router.get("/:userId", getSavedTranslations);
router.put("/update", updateSavedTranslations);
router.delete("/delete/:translationId", deleteSavedTranslations);

export default router;
 */
